{% extends 'base.html.twig' %}

{% block title %}Produitvendu{% endblock %}

{% block body %}
    <h1>Produitvendu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produitvendu.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('produitvendu_index') }}">back to list</a>

    <a href="{{ path('produitvendu_edit', {'id': produitvendu.id}) }}">edit</a>

    {{ include('produitvendu/_delete_form.html.twig') }}
{% endblock %}
