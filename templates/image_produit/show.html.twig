{% extends 'base.html.twig' %}

{% block title %}ImageProduit{% endblock %}

{% block body %}
    <h1>ImageProduit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ image_produit.id }}</td>
            </tr>
            <tr>
                <th>Domaine</th>
                <td>{{ image_produit.domaine }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('image_produit_index') }}">back to list</a>

    <a href="{{ path('image_produit_edit', {'id': image_produit.id}) }}">edit</a>

    {{ include('image_produit/_delete_form.html.twig') }}
{% endblock %}
