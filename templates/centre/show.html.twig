{% extends 'base.html.twig' %}

{% block title %}Centre{% endblock %}

{% block body %}
    <h1>Centre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ centre.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ centre.nom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ centre.adresse }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ centre.tel }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ centre.email }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ centre.ville }}</td>
            </tr>
            <tr>
                <th>Pays</th>
                <td>{{ centre.pays }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('centre_index') }}">back to list</a>

    <a href="{{ path('centre_edit', {'id': centre.id}) }}">edit</a>

    {{ include('centre/_delete_form.html.twig') }}
{% endblock %}
